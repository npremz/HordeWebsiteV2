---
import ContactForm from "../ui/forms/ContactForm";
import type { FormTranslations } from "../ui/forms/ContactForm";
import { formatHeadingHtml } from "../../utils/heading-html";
import type { ServiceData } from "./types";

interface Props {
  service: ServiceData;
  formTranslations: FormTranslations;
}

const { service, formTranslations } = Astro.props;
---

<section id="service-cta" class="mt-20 sm:mt-25 md:mt-50" data-service-section="cta" aria-labelledby="service-cta-title">
  <h2 id="service-cta-title" class="text-[1.5rem] sm:text-[2rem] md:text-[2.5rem] leading-none flex pb-10">
    <span set:html={formatHeadingHtml(service.ctaTitle)} />
  </h2>
  <div>
    {service.ctaParagraphs.map((paragraph) => {
      const emailRegex = /(hello@hordeagence\.com)/;
      const parts = paragraph.split(emailRegex);
      return (
        <p class="text-[1.125rem] max-w-[900px]">
          {parts.map((part) =>
            part === 'hello@hordeagence.com'
              ? <a class="cta-email-link" href="mailto:hello@hordeagence.com">{part}</a>
              : part
          )}
        </p>
      );
    })}

    <div id="contact-form" class="mt-25">
      <ContactForm
        client:load
        translations={formTranslations}
        variant="light"
        hideNeedField={true}
        source={{
          type: 'service-page',
          pagePath: service.seoUrl,
          serviceSlug: service.slug,
        }}
      />
    </div>
  </div>
</section>

<style>
  .cta-email-link {
    position: relative;
    display: inline-block;
    border-bottom: 1px solid rgb(0 0 0 / 0.3);
  }

  .cta-email-link::after {
    content: "";
    position: absolute;
    bottom: -1px;
    left: 0;
    width: 100%;
    height: 1px;
    background-color: currentColor;
    transform: scaleX(0);
    transform-origin: left center;
    transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  }

  .cta-email-link:hover::after {
    transform: scaleX(1);
  }
</style>
