services:
  hordev2:
    build: .
    container_name: ${CONTAINER_NAME:-hordenew}
    restart: unless-stopped
    ports:
      - "${PORT:-4328}:${PORT:-4328}"
    environment:
      - PORT=${PORT:-4328}
      - RESEND_API_KEY=${RESEND_API_KEY}
